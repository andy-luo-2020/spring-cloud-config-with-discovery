server:
  port: ${PORT:8083}  

eureka:
  instance:
    metadata-map:
      zone: zone3
  client:
    service-url:
      defaultZone: http://localhost3:8763/eureka/,http://localhost1:8761/eureka/,http://localhost2:8762/eureak/