server:
  port: ${PORT:8763}  
          
eureka:
  instance:
    hostname: localhost3
    metadata-map:
      zone: zone3
  client:
    service-url:
      defaultZone: http://localhost1:8761/eureka/,http://localhost2:8762/eureka/