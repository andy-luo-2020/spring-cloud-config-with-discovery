server:
  port: ${PORT:8762}      

eureka:
  instance:
    hostname: localhost2
    metadata-map:
      zone: zone2
  client:
    service-url:
      defaultZone: http://localhost1:8761/eureka/,http://localhost3:8763/eureka/